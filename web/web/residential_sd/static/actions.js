import { api } from '../../static/js/network_client';

// TODO: backend implemantation is missing - similar to the data for power dispatch page, but need to identify which demand/supply
// export function getPVSystemData() {
//     return dispatch => {
//         api.get('', (response) => {
//             dispatch(capacitySystemLoadDataUpdated(response.results.data));
//         }, (error) => {
//             console.warn(error);
//         });
//     };
// }
